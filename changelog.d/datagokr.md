- **core(datagokr)**: data.go.kr 게이트웨이 공유 헬퍼 `arcsolve/services/_datagokr.py` 도입 — 6개 서비스(airkorea·egen·ev_charger·parking·tago_transit·airport)가 각자 재구현하던 **표준 결과/에러 코드표·numOfRows 클램프**를 단일 출처로 상환. `RESULT_CODE_OK`·`RESULT_CODE_HINTS`(data.go.kr OpenAPI 공통 cmmMsgHeader/resultCode canonical 표 — 00·01·02·03·04·05·10·11·12·20·21·22·30·31·32·33·99 전체)·`explain_result_code(code, msg)`(정상/None→None, 알 수 없는 코드는 code/msg 보존)·`clamp_paging(num_of_rows, page_no, *, max_rows, min_rows, min_page)` 제공. 감사에서 절반의 서비스가 누락하던 코드(05 연결실패/타임아웃·10 잘못된 파라미터·11 필수 파라미터 누락·21 일시적 키 비활성)가 **이제 6서비스 전부에서 일관 안내**된다. 봉투 파싱 구조(JSON `response.header`·top-level·XML `header`)는 서비스별로 그대로 두고 "코드→메시지 해석"과 "클램프"만 공유. 클램프가 없던 5서비스(ev_charger 제외)에 `clamp_paging` 적용해 `numOfRows`/`pageNo`를 안전 범위로 검증(상한 미확정 서비스는 게이트웨이 통용 9999를 보수적 상한으로 두고 `TODO(provenance)` 표기). 서비스별 특화 문구(airkorea·airport의 개발계정 일 500건 안내)는 유지. `tests/test_datagokr.py` 추가.
